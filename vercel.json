{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd build && npm install",
  "buildCommand": "cd build && npm run build",
  "outputDirectory": "build/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}

{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM"],
    "types": ["node"],
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "verbatimModuleSyntax": false,
    "noEmit": true
  },
  "include": ["engine", "tests"],
  "comment_exclude": "This test reaches the app's pricing/adapter layer (toProject → @mebelchi/* + store's import.meta.env), which only the app's Vite build types. It runs in vitest (aliases resolve there); root tsc can't type it, so keep it out of the root program.",
  "exclude": ["tests/rich_cell_characterization.test.ts"]
}

node_modules/
*.log
.DS_Store
mined_*
joint_decisions*.json
dist/
.vite/
packages/*/dist/

# Multi-agent coordination live state (config.json is committed)
active_tasks.md
active_files.md

# Internal karkas plan + review handoff — local only, must not appear on GitHub
KARKAS_ROADMAP.md
ANTIGRAVITY_REVIEW.md

# Claude Code local tooling / IDE config (user request 2026-07-18) — not app code, keep out of the repo
.claude/

# --- Non-app material: research, business, UI examples, data dumps ---
# (kept locally; hidden from GitHub)
MEBELCHI_INVESTMENT_MEMO.md
ENGINE_README.md
PERF_LEDGER.md
price_list_chin_wood.xlsx
joint_decisions.unmatched.txt
Researches/
UI Examples liked/
UI Exploration/
UI Handover/
UI image Examples/
Example sets/
Print Examples/
XML output examples/
sample_exports/
Handover 03/
Deck/
catalog/
DB/
CORE 1/
CORE 2/
CORE 3/
bench/
mined/

# Archived: the standalone karkas prototype (Expo / react-native-web). Superseded by apps/app +
# root engine/ (the port is complete; no live code crosses into it). Kept locally for its puppeteer
# screenshot harness; safe to delete for disk space. Ships nothing — never track or deploy it.
karkas-app/

# Local screen recordings / frame dumps — reference material, not app assets.
*.mp4
*.mp3
Video Frame Extractor*/

# Founder internal spec/handover docs — keep local, not in the public repo (user request 2026-07-08)
CONSTRUCTION_FRAME_v4.md
HANDOVER_04_SAIDISLOM.md
# Founder 3D-construction task drop (2026-07-17) — internal spec, local-only
TASK_3D_CONSTRUCTION.md
27_DESIGN_CONSTRUCTION_SEPARATION.md
29_TEAM_STRUCTURE.md
30_ENGINE_STATUS.md

# Vercel CLI link dir
.vercel
# Vercel env pulls at the repo root (`vercel env pull`) — these carry BLOB_READ_WRITE_TOKEN and friends.
# The repo is PUBLIC, so an untracked-by-accident .env here would publish a live secret.
.env
.env.local
.env.*.local

# Generated HTML deliverables (2026-07-24). These are REBUILDABLE from the scratchpad scripts and run to
# tens of megabytes of inlined PNG, which does not belong in a public repo. Keep them local; regenerate
# with the gallery/guide builders when they are needed again. Any future generated .html artifact of this
# kind belongs here too — a small text report may still be committed on purpose.
GALEREYA_50.html
QOLLANMA_MEBEL_YASASH.html
